import './index.css'
import React from 'react'
import ReactDOM from 'react-dom'

import App from './App.jsx'

// ReactDOM.render recebe o(s) elemento(s) que o react deve renderizar na página
ReactDOM.render (
    <App/>,
    document.getElementById('root')
)